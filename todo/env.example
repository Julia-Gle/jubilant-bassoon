# Datenbank-Auswahl (memory, sqlite, postgresql, mongodb)
DB_TYPE=memory

# SQLite-Konfiguration (für In-Memory oder Datei-basiert)
SQLITE_PATH=:memory:

# PostgreSQL-Konfiguration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=todo_app
DB_USER=postgres
DB_PASSWORD=password

# MongoDB-Konfiguration
MONGODB_URI=mongodb://localhost:27017/todo_app

# Server-Konfiguration
PORT=3000
NODE_ENV=development

# JWT-Konfiguration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
